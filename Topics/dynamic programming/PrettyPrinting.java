class PrettyPrinting{
    int minCost(List<String> n, int length){
        int[] dp = new int[n.size() + 1];
        for(int i = 1; i < n; i++){
            int remSpaces = length;
            int mess = Integer.MAX_VALUE;
            for(int j = i; j >= 0; j--){
                remSpaces
            }
        }
    }
}